export class Product {
  constructor({ id, brand, model, price, imageUrl }) {
    this.id = id;
    this.brand = brand;
    this.model = model;
    this.price = price;
    this.imageUrl = imageUrl;
  }
}
